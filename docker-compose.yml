version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: laravel-app
    container_name: laravel-app
    ports:
      - "8000:80"
    volumes:
      - .:/var/www
    env_file:
      - .env
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
